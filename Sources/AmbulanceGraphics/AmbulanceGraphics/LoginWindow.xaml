<Window x:Class="AmbulanceGraphics.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AmbulanceGraphics"
        mc:Ignorable="d"
        Title="LoginWindow" Height="150" Width="300" Loaded="Window_Loaded">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Label Content="Потребителско име" VerticalAlignment="Center" HorizontalAlignment="Right" />
        <TextBox Name="txtUserName" Grid.Column="1" VerticalAlignment="Center"/>
        <Label Content="Парола" Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Right" />
        <PasswordBox Name="txtPassword" Grid.Row="1" Grid.Column="1" VerticalAlignment="Center"/>
        <Button Content="Влез" Grid.Row="2" Name="btnLogin" Click="btnLogin_Click" IsDefault="True"/>
        <Button Content="Отказ" Grid.Row="2" Grid.Column="1" Name="btnCancel" Click="btnCancel_Click"/>
    </Grid>
</Window>
